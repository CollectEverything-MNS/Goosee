apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: goosee-dev
spec:
  selector:
    app: api-gateway
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30001
  type: NodePort
