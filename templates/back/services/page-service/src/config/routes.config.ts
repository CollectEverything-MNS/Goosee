export const pagesRoutes = {
  root: `/pages`,
  pages: {
    byId: `/:id`,
    seed: `/seed`,
    bySlug: `/slug/:slug`,
  },
};

export const menusRoutes = {
  root: `/menus`,
  menus: {
    byId: `/:id`,
    reorder: `/reorder`,
  },
};

export const settingsRoutes = {
  root: `/settings`,
};
