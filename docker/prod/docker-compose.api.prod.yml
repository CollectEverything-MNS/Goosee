services:
    api-gateway-prod:
        build:
            context: ../templates/back/api-gateway
            dockerfile: Dockerfile
        command: node dist/main
        ports:
            - "3001:3000"
        environment:
            NODE_ENV: ${NODE_ENV}
        networks:
            - goosee_net
