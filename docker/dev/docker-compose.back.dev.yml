services:
  goosee-api-gateway-dev:
    container_name: goosee-api-gateway-dev
    build:
      context: ../..
      dockerfile: templates/back/api-gateway/Dockerfile.dev
    command: yarn dev
    ports:
      - "${API_GATEWAY_PORT}:${API_GATEWAY_PORT}"
    environment:
      NODE_ENV: ${NODE_ENV}
      AUTH_SERVICE_HOST: goosee-auth-service-dev
      AUTH_SERVICE_PORT: ${AUTH_SERVICE_PORT}
      JWT_SECRET: ${JWT_SECRET}
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      PAGE_SERVICE_HOST: goosee-page-service-dev
      PAGE_SERVICE_PORT: ${PAGE_SERVICE_PORT}
      USER_SERVICE_PORT: ${USER_SERVICE_PORT}
      USER_SERVICE_HOST: goosee-user-service-dev
      LOG_SERVICE_HOST: goosee-log-service-dev
      LOG_SERVICE_PORT: ${LOG_SERVICE_PORT}
      PRODUCT_SERVICE_HOST: goosee-product-service-dev
      PRODUCT_SERVICE_PORT: ${PRODUCT_SERVICE_PORT}
      ORDER_SERVICE_HOST: goosee-order-service-dev
      ORDER_SERVICE_PORT: ${ORDER_SERVICE_PORT}
      CART_SERVICE_HOST: goosee-cart-service-dev
      CART_SERVICE_PORT: ${CART_SERVICE_PORT}
      NEXT_PUBLIC_WEB_URL: ${NEXT_PUBLIC_WEB_URL}
    depends_on:
      - goosee-auth-service-dev
    networks:
      - goosee_net

  goosee-auth-service-dev:
    container_name: goosee-auth-service-dev
    build:
      context: ../..
      dockerfile: templates/back/services/auth-service/Dockerfile.dev
    command: yarn dev
    environment:
      NODE_ENV: ${NODE_ENV}
      RABBITMQ_URL: amqp://guest:guest@goosee-rabbitmq-dev:5672
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      AUTH_SERVICE_PORT: ${AUTH_SERVICE_PORT}
      JWT_SECRET: ${JWT_SECRET}
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      JWT_ACCESS_EXPIRES_IN: ${JWT_ACCESS_EXPIRES_IN}
      JWT_REFRESH_EXPIRES_IN: ${JWT_REFRESH_EXPIRES_IN}
      AUTH_DB_HOST: goosee-auth-db-dev
      AUTH_DB_PORT: 5432
      AUTH_DB_USER: ${AUTH_DB_USER}
      AUTH_DB_PASSWORD: ${AUTH_DB_PASSWORD}
      AUTH_DB_NAME: ${AUTH_DB_NAME}
    expose:
      - "${AUTH_SERVICE_PORT}"
    depends_on:
      goosee-auth-db-dev:
        condition: service_healthy
      goosee-rabbitmq-dev:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost ${AUTH_SERVICE_PORT}"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    networks:
      - goosee_net

  goosee-page-service-dev:
    container_name: goosee-page-service-dev
    build:
      context: ../..
      dockerfile: templates/back/services/page-service/Dockerfile.dev
    command: yarn dev
    environment:
      NODE_ENV: ${NODE_ENV}
      RABBITMQ_URL: amqp://guest:guest@goosee-rabbitmq-dev:5672
      PAGE_SERVICE_PORT: ${PAGE_SERVICE_PORT}
      PAGE_DB_HOST: goosee-page-db-dev
      PAGE_DB_PORT: 5432
      PAGE_DB_USER: ${PAGE_DB_USER}
      PAGE_DB_PASSWORD: ${PAGE_DB_PASSWORD}
      PAGE_DB_NAME: ${PAGE_DB_NAME}
      MINIO_ENDPOINT: goosee-minio-dev
      MINIO_PORT: ${MINIO_PORT}
      MINIO_USE_SSL: ${MINIO_USE_SSL}
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET: ${MINIO_BUCKET}
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL}
    expose:
      - "${PAGE_SERVICE_PORT}"
    depends_on:
      goosee-page-db-dev:
        condition: service_healthy
      goosee-rabbitmq-dev:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost ${PAGE_SERVICE_PORT}"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    networks:
      - goosee_net

  goosee-notifier-service-dev:
    container_name: goosee-notifier-service-dev
    build:
      context: ../..
      dockerfile: templates/back/services/notifier-service/Dockerfile.dev
    command: yarn dev
    environment:
      NODE_ENV: ${NODE_ENV}
      RABBITMQ_URL: amqp://guest:guest@goosee-rabbitmq-dev:5672
      SMTP_HOST: goosee-mailhog-dev
      SMTP_PORT: ${SMTP_PORT}
      SMTP_SECURE: ${SMTP_SECURE}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      SMTP_FROM: ${SMTP_FROM}
    depends_on:
      goosee-rabbitmq-dev:
        condition: service_healthy
      goosee-mailhog-dev:
        condition: service_started
    restart: unless-stopped
    networks:
      - goosee_net

  goosee-user-service-dev:
    container_name: goosee-user-service-dev
    build:
      context: ../..
      dockerfile: templates/back/services/user-service/Dockerfile.dev
    command: yarn dev
    environment:
      NODE_ENV: ${NODE_ENV}
      RABBITMQ_URL: amqp://guest:guest@goosee-rabbitmq-dev:5672
      USER_SERVICE_PORT: ${USER_SERVICE_PORT}
      USER_DB_HOST: goosee-user-db-dev
      USER_DB_PORT: 5432
      USER_DB_USER: ${USER_DB_USER}
      USER_DB_PASSWORD: ${USER_DB_PASSWORD}
      USER_DB_NAME: ${USER_DB_NAME}
    expose:
      - "${USER_SERVICE_PORT}"
    depends_on:
      goosee-user-db-dev:
        condition: service_healthy
      goosee-rabbitmq-dev:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost ${USER_SERVICE_PORT}"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    networks:
      - goosee_net

  goosee-log-service-dev:
    container_name: goosee-log-service-dev
    build:
      context: ../..
      dockerfile: templates/back/services/log-service/Dockerfile.dev
    command: yarn dev
    environment:
      NODE_ENV: ${NODE_ENV}
      RABBITMQ_URL: amqp://guest:guest@goosee-rabbitmq-dev:5672
      LOG_SERVICE_PORT: ${LOG_SERVICE_PORT}
      LOG_DB_HOST: goosee-log-db-dev
      LOG_DB_PORT: 5432
      LOG_DB_USER: ${LOG_DB_USER}
      LOG_DB_PASSWORD: ${LOG_DB_PASSWORD}
      LOG_DB_NAME: ${LOG_DB_NAME}
    expose:
      - "${LOG_SERVICE_PORT}"
    depends_on:
      goosee-log-db-dev:
        condition: service_healthy
      goosee-rabbitmq-dev:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost ${LOG_SERVICE_PORT}"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    networks:
      - goosee_net

  goosee-product-service-dev:
    container_name: goosee-product-service-dev
    build:
      context: ../..
      dockerfile: templates/back/services/product-service/Dockerfile.dev
    command: yarn dev
    environment:
      NODE_ENV: ${NODE_ENV}
      RABBITMQ_URL: amqp://guest:guest@goosee-rabbitmq-dev:5672
      PRODUCT_SERVICE_PORT: ${PRODUCT_SERVICE_PORT}
      PRODUCT_DB_HOST: goosee-product-db-dev
      PRODUCT_DB_PORT: 5432
      PRODUCT_DB_USER: ${PRODUCT_DB_USER}
      PRODUCT_DB_PASSWORD: ${PRODUCT_DB_PASSWORD}
      PRODUCT_DB_NAME: ${PRODUCT_DB_NAME}
      MINIO_ENDPOINT: goosee-minio-dev
      MINIO_PORT: ${MINIO_PORT}
      MINIO_USE_SSL: ${MINIO_USE_SSL}
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET: ${MINIO_BUCKET}
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL}
    expose:
      - "${PRODUCT_SERVICE_PORT}"
    depends_on:
      goosee-product-db-dev:
        condition: service_healthy
      goosee-rabbitmq-dev:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost ${PRODUCT_SERVICE_PORT}"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    networks:
      - goosee_net

  goosee-order-service-dev:
    container_name: goosee-order-service-dev
    build:
      context: ../..
      dockerfile: templates/back/services/order-service/Dockerfile.dev
    command: yarn dev
    environment:
      NODE_ENV: ${NODE_ENV}
      ORDER_SERVICE_PORT: ${ORDER_SERVICE_PORT}
      ORDER_DB_HOST: goosee-order-db-dev
      ORDER_DB_PORT: 5432
      ORDER_DB_USER: ${ORDER_DB_USER}
      ORDER_DB_PASSWORD: ${ORDER_DB_PASSWORD}
      ORDER_DB_NAME: ${ORDER_DB_NAME}
    expose:
      - "${ORDER_SERVICE_PORT}"
    depends_on:
      goosee-order-db-dev:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost ${ORDER_SERVICE_PORT}"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    networks:
      - goosee_net

  goosee-cart-service-dev:
    container_name: goosee-cart-service-dev
    build:
      context: ../..
      dockerfile: templates/back/services/cart-service/Dockerfile.dev
    command: yarn dev
    environment:
      NODE_ENV: ${NODE_ENV}
      CART_SERVICE_PORT: ${CART_SERVICE_PORT}
      CART_DB_HOST: goosee-cart-db-dev
      CART_DB_PORT: 5432
      CART_DB_USER: ${CART_DB_USER}
      CART_DB_PASSWORD: ${CART_DB_PASSWORD}
      CART_DB_NAME: ${CART_DB_NAME}
    expose:
      - "${CART_SERVICE_PORT}"
    depends_on:
      goosee-cart-db-dev:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost ${CART_SERVICE_PORT}"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    networks:
      - goosee_net

networks:
  goosee_net:
    name: goosee_net
    driver: bridge